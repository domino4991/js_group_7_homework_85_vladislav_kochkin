const initialState = {
    albums: null,
    error: null
};

export const albumsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
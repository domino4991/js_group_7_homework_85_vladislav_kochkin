const initialState = {
    tracks: null,
    error: null
};

export const tracksReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};